serviceAccount:
  # Specifies whether a service account should be created
  # Annotations to add to the service account
  annotations: 
    test: "ref+awsssm://johnny/test"

configMap: |
  some stuff with "ref+awsssm://johnny/test"